TCC xattr module
================

Reports whether or not the extended attribute `com.apple.rootless` is set to
the expected value `TCC` on `/Library/Application Support/com.apple.TCC`.

The table provides the following information per 'item':

* id (int) Unique id
* serial_number (string) Serial Number
* status (string) One of `present` or `absent`.
